import CreatePageClient from './CreatePageClient';

export const metadata = {
  title: {
    absolute: 'Create Post | Azelin Blog',
  },
};

export default function CreatePostPage() {
  return <CreatePageClient />;
}